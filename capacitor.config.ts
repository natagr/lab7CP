import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.lab1',
  appName: 'lab1',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
